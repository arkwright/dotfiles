" Disable default Command+p mapping (which MacVim uses to print the current
" file), to prevent a conflict with a desired YankRing Command+p mapping
" (specified in .vimrc).
:macmenu File.Print key=<nop>
